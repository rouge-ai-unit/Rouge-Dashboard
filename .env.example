# Copy to .env.local (dev) or set in your hosting provider (prod). Never commit real secrets.

# --- Core runtime ---
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database (Neon, local Postgres, or any managed provider)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# --- Auth ---
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme-strong-secret

# Google OAuth (preferred in prod)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Dev bypass (never enable in prod)
# NEXT_PUBLIC_DISABLE_AUTH=true

# --- Authorization allowlists (production recommended) ---
# Comma-separated exact emails and/or domains and/or regex patterns.
ALLOWED_EMAILS=
ALLOWED_DOMAINS=yourcompany.com
ALLOWED_EMAIL_PATTERNS=

# --- Analytics (optional) ---
# GA4 property ID for /api/analytics
GA4_PROPERTY_ID=

# --- Generative AI (optional) ---
# Server-side Gemini API key; enables /api/contents/generate
NEXT_PUBLIC_GEMINI=

# Optional client analytics id
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=

# --- Feature flags / dev helpers ---
# Set to true only in local dev to bypass auth checks.
# Do not enable in production.
NEXT_PUBLIC_DISABLE_AUTH=false
